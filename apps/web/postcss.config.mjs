/**
 * Tailwind v4 is a PostCSS plugin. `postcss` itself is not a direct dependency
 * — @tailwindcss/postcss brings the version it needs.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
